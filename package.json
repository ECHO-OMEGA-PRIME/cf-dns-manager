{
  "name": "cf-dns-manager",
  "version": "1.0.0",
  "description": "Cloudflare Worker for DNS management API",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
